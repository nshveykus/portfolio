| № | Задача | Название файла | Освоенные навыки |
| :--- | :--- | :--- | :--- |
| 1 | Поиск дубликатов | `01_find_duplicates.sql` | `GROUP BY`, `HAVING`, поиск дубликатов в данных |
| 2 | Поиск заказов без адреса | `02_null_addresses.sql` | Проверка на `NULL` и пустые строки (`IS NULL`, `OR`), сортировка (`ORDER BY`) |
| 3 | Поиск заказов, которые ссылаются на несуществующих пользователей | `03_orphan_orders.sql` | `LEFT JOIN`, поиск "сирот" (записей без связи), проверка внешних ключей |
| 4 |  Проверка фильтрации по дате | `04_date_filter.sql` | Фильтрация по диапазону дат (`BETWEEN`), работа с `TIMESTAMP`, учет времени в датах |
| 5 | Топ-5 популярных товаров | `05_top_products.sql` | Многотабличные `JOIN`, агрегация (`SUM`), сортировка (`ORDER BY`), `LIMIT` |
| 6 | Подсчет суммы корзины | `06_cart_total.sql` | Вычисляемые поля (`quantity * price`), фильтрация по внешнему ключу, `SUM` с арифметикой |
| 7 | Найти где заказ доставлен, но не оплачен | `07_illogical_statuses.sql` | Условная логика в `WHERE` (сочетание нескольких условий), проверка бизнес-правил |
| 8 | Проверка доли отменённых заказов | `08_cancel_rate.sql` | Условная агрегация (`SUM(CASE WHEN...)`), вычисление процентов, защита от целочисленного деления |
| 9 | Поиск давно неактивных пользователей | `09_inactive_users.sql` | Работа с датами (`NOW() - INTERVAL`), комбинирование условий (`AND`, `IS NULL`) |
| 10 | Показать заказы с шестого по десятый | `10_pagination.sql` | Пагинация (`LIMIT`, `OFFSET`), обязательность `ORDER BY` при пагинации |
| 11 | Клиенты с более чем одним заказом | `11_order_number.sql` | Оконные функции (`ROW_NUMBER() OVER(PARTITION BY ...)`), `CTE` (WITH), вложенная агрегация |
| 12 | Товары, которые никто не заказывал | `12_unsold_products.sql` | `LEFT JOIN` для поиска отсутствующих записей, альтернатива через `NOT EXISTS` |
| 13 | Подсчет разницы между вчерашней выручкой и средней | `13_daily_revenue_analysis.sql` | Многоуровневые `CTE`, `CROSS JOIN`, защита от `NULL` (`COALESCE`, `NULLIF`), работа с датами (`CURDATE`) |
| 14 | Поиск пропусков нумерации заказов | `14_missing_ids.sql` | Рекурсивные `CTE` (`WITH RECURSIVE`), генерация последовательностей, поиск пропусков в нумерации |
| 15 | В какое время суток пользователи чаще всего оформляют заказы | `15_peak_hours.sql` | Извлечение частей даты (`HOUR`), `LEFT JOIN` с сгенерированным календарем, группировка по вычисляемому полю |
